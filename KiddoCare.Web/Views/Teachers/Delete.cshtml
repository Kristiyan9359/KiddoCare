@model KiddoCare.ViewModels.Teachers.TeacherDeleteViewModel

@{
    ViewData["Title"] = T["Delete Teacher"];
    var backUrl = Model.ReturnUrl ?? Url.Action("Index");
}

<section class="delete-page">
    <div class="delete-card">
        <div class="delete-kicker">@T["Permanent action"]</div>
        <h1>@T["Delete Teacher"]</h1>
        <p>
            @T["Are you sure you want to delete this teacher profile? This action cannot be undone."]
        </p>

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <dl class="info-list delete-info-list">
            <div>
                <dt>@T["Full Name"]</dt>
                <dd>@Model.FullName</dd>
            </div>
            <div>
                <dt>@T["Email"]</dt>
                <dd>@Model.Email</dd>
            </div>
            <div>
                <dt>@T["Assigned Group"]</dt>
                <dd>@Model.GroupName</dd>
            </div>
        </dl>

        <form asp-action="Delete" method="post" class="form-action-row justify-content-center">
            <input asp-for="Id" type="hidden" />
            <input asp-for="ReturnUrl" type="hidden" />

            <button type="submit" class="btn btn-danger">@T["Delete"]</button>
            <a href="@backUrl" class="btn btn-secondary">@T["Cancel"]</a>
        </form>
    </div>
</section>
